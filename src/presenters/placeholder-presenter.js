import Presenter from './presenter.js';

/**
 * @extends {Presenter<PlaceholderView>}
 */
class PlaceholderPresenter extends Presenter {
  /**
   * @override
   */
  createViewState() {
    // TODO: PlaceholderViewState
  }
}

export default PlaceholderPresenter;
